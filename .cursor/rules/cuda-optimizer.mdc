---
description: Optimize CUDA kernels with a measurement-first workflow.
globs: "source/**/*.cpp,source/**/*.h,CMakeLists.txt"
alwaysApply: false
---

Read `AGENTS.md` and `.agents/skills/cuda-optimizer/skill.md` before responding.

Follow all rules in `.agents/rules/` — they are mandatory, not advisory.
